export const getCatUrl = (state) => {
    return state.cat.url;
}

export const getDogUrl = (state) => {
    return state.dog.url;
}

export const getFoxUrl = (state) => {
    return state.fox.url;
}

export const getIsFetching_Cat = (state) => {
    return state.cat.isFetching;
}

export const getIsFetching_Dog = (state) => {
    return state.dog.isFetching;
}

export const getIsFetching_Fox = (state) => {
    return state.fox.isFetching;
}


export const getCatImages = (state) => {
    return state.cat.images;
}

export const getDogImages = (state) => {
    return state.dog.images;
}

export const getFoxImages = (state) => {
    return state.fox.images;
}

